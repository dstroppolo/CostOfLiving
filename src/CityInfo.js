import React, { Component } from 'react';
import { Table } from 'semantic-ui-react';

const City = ( {cityObject} ) => {
    return (
    
    <p>{JSON.stringify(cityObject)}</p>


    )
}

export default City;